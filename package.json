{
  "name": "jeffpowell-monorepo",
  "description": "Monorepo for Jeff Powell's Cloudflare workers",
  "version": "1.0.0",
  "author": "Jeff Powell",
  "license": "MIT",
  "private": true,
  "scripts": {
    "build": "pnpm -r run build",
    "build-prod": "pnpm -r run build-prod",
    "deploy": "pnpm -r run deploy",
    "deploy-dev": "pnpm -r run deploy-dev"
  }
}
